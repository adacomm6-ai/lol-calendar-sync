'use client';

export default function SafeReviewClient(props: any) {
    return (
        <div className="p-10 border border-blue-500 text-blue-500">
            SAFE CLIENT COMPONENT LOADED
        </div>
    );
}
